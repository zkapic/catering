{
    "builds": [{
        "src": "catering/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "catering/wsgi.py"
        }
    ]
}